<div class="container">
  <h2 class="center-align">Ofertas de Empleo</h2>
</div>

<div class="divider"></div>
<div class="container">
  <div class="row">
    <div class="col s4 m4 l2">
      <h5 class="left-align">
        <%= link_to 'Nuevo', new_announcement_path, class: "btn waves-effect waves-light" %>
      </h5>
    </div>
    <div class="col s8 m4 l10">
      <table class="striped">
        <thead>
          <tr>
            <th>Fecha Publicacion</th>
            <th>Fecha Limite</th>
            <th>Tipo Oferta</th>
            <th>Informacion</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @announcements.each do |announcement| %>
            <tr>
              <td><%= announcement.publicationDate %></td>
              <td><%= announcement.deadline %></td>
              <td><%= announcement.announcementType %></td>
              <td><%= announcement.announcementInformation %></td>
              <td><%= link_to 'Ver', announcement %></td>
              <% if current_user.try(:admin?) %>
                <td><%= link_to 'Editar', edit_announcement_path(announcement) %></td>
                <td><%= link_to 'Eliminar', announcement, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
      <div class="center-align">
        <%= will_paginate @announcements,  renderer: MaterializePagination::Rails %>
      </div>

    </div>
  </div>
</div>

<br>
