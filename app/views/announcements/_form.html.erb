<%= form_with(model: announcement, local: true) do |form| %>
  <% if announcement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(announcement.errors.count, "error") %> prohibited this announcement from being saved:</h2>

      <ul>
      <% announcement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idAnnouncement %>
    <%= form.number_field :idAnnouncement, id: :announcement_idAnnouncement %>
  </div>

  <div class="field">
    <%= form.label :publicationDate %>
    <%= form.date_select :publicationDate, id: :announcement_publicationDate %>
  </div>

  <div class="field">
    <%= form.label :deadline %>
    <%= form.date_select :deadline, id: :announcement_deadline %>
  </div>

  <div class="field">
    <%= form.label :announcementType %>
    <%= form.text_area :announcementType, id: :announcement_announcementType %>
  </div>

  <div class="field">
    <%= form.label :announcementInformation %>
    <%= form.text_field :announcementInformation, id: :announcement_announcementInformation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
