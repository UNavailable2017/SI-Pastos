<%= form_with(model: work_group, local: true) do |form| %>
  <% if work_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_group.errors.count, "error") %> prohibited this work_group from being saved:</h2>

      <ul>
      <% work_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :participant %>
    <%= form.text_area :participant, id: :work_group_participant %>
  </div>

  <div class="field">
    <%= form.label :proyect %>
    <%= form.text_area :proyect, id: :work_group_proyect %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
