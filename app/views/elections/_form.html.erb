<%= form_with(model: election, local: true) do |form| %>
  <% if election.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(election.errors.count, "error") %> prohibited this election from being saved:</h2>

      <ul>
      <% election.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idElection %>
    <%= form.number_field :idElection, id: :election_idElection %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :election_date %>
  </div>

  <div class="field">
    <%= form.label :winner %>
    <%= form.text_field :winner, id: :election_winner %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
