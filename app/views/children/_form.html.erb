<%= form_with(model: child, local: true) do |form| %>
  <% if child.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(child.errors.count, "error") %> prohibited this child from being saved:</h2>

      <ul>
      <% child.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idChild %>
    <%= form.number_field :idChild, id: :child_idChild %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :child_name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age, id: :child_age %>
  </div>

  <div class="field">
    <%= form.label :censo_id %>
    <%= form.number_field :censo_id, id: :child_censo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
