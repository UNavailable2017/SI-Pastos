<div class="container">
  <div class="row">
    <div class="col s12">
      <div class="center-align">
        <%= link_to image_tag("escudo.png", height: "200"), root_path %>
      </div>
      <h2 class="center-align">¿Olvidaste tu contraseña?</h2>
    </div>
  </div>

  <div class="row">
    <div class="col m6 offset-m3 z-depth-5">
      <div class="card-form">
        <p>Indica tu correo electrónico a continuación y te enviaremos las instrucciones para cambiar tu contraseña.</p>
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
          <%= devise_error_messages! %>

          <div class="field">
            <%= f.label :email, 'Correo electrónico:' %><br />
            <%= f.email_field :email, autofocus: true %>
          </div>

          <div class="actions center-align">
            <%= f.submit "Enviarme una contraseña de reinicio", class: "waves-effect waves-light btn" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

</div>
